# Handover

**Last updated:** 2026-06-10 (JST)
**Updated by:** claude (AI draft from git history — review before trusting)

## Current State

The library and CLI are functional per the MY-39x/MY-41x issue series:
project structure (#1), cherry-picked OpenAPI feedback endpoints (#2),
oapi-codegen generation (#3), client wrapper with auth/retry/error handling
(#4), CLI feedback subcommands (#6), helpers (#10), stdin pipe mode (#11),
contract tests with the Bearer-to-Basic auth fix (#12, #14), E2E tests for
the CLI binary (#13), constructor injection for NewClient (#16), and the
required `--weave-ref` flag (#17). The module was renamed to weaveback with
E2E fixes in #18. Last commit: `d53641f` "docs: add decision queue for
human-review items (#19)" on 2026-06-10.

## In Progress

不明 (git 履歴からは判別できず) — no open feature branch is evident in the
shallow clone.

## Next Actions

1. requester による docs/intent.md ドラフトのレビューと確定
2. Configure `just fetch-spec` — currently a stub that exits 1 ("not yet configured (see MY-393)")
3. Work through the human-review items in `docs/decision-queue.md` (added 2026-06-10, #19)

## Known Risks / Blockers

- `just fetch-spec` is unimplemented, so refreshing `api/openapi.yaml` from upstream Weave is a manual/undefined process
- Integration tests need a real `WANDB_API_KEY`; they are not runnable offline

## Context the Next Actor Needs

- Auth is HTTP Basic, not Bearer — this was a deliberate fix (#12, #14); don't "correct" it back
- `pkg/weave/gen/` is generated by oapi-codegen from `api/` specs via `just generate`; edit the spec or the generator config (`api/oapi-codegen-*.yaml`), never the generated files
- `NewClient` takes its config via constructor injection (no implicit `os.Getenv`) per #16
- Tests are split: `tests/unit/`, `tests/integration/` (needs `WANDB_API_KEY`), `tests/e2e/` (runs the built CLI binary)
- Issue IDs in commit messages (MY-xxx) refer to a Linear project

## Relevant Files and Commands

- `pkg/weave/` — client library; `pkg/weave/gen/` — generated code (do not edit)
- `cmd/weaveback/` — CLI entrypoint
- `api/openapi.yaml` + `api/oapi-codegen-*.yaml` — spec and generator configs
- `docs/decision-queue.md` — open human-review items
- `justfile` — `just build`, `just test`, `just lint`, `just generate`, `just test-integration`
